zsystem:
  hosts:
    192.86.33.143:
      ansible_port: "{{ lookup('env', 'hostport') }}"
      ansible_user: "{{ lookup('env', 'hostuser') }}"
      ansible_password: "{{ lookup('env', 'hostpassword') }}"
      ansible_ssh_pipelining: true
